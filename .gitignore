node_modules
dist
dist-ssr
*.local
.vite

# Local credentials/configuration. Release signing keys belong in CI secrets.
.env
.env.*
!.env.example

# Rust / Tauri
src-tauri/target
src-tauri/gen

# Assets (kept locally only)
ChatGPT Image *.png

# Built binaries (dev artifacts)
transloop.exe
uninstall.exe

# Copyright registration docs (generated, large)
scripts/
