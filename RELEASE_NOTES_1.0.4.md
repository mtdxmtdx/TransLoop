# TransLoop 1.0.4

## 定位

TransLoop 1.0.4 是面向 Windows 桌面划词翻译与截图 OCR 翻译的修复更新，重点改善截图翻译的目标语言一致性、视觉模型输出处理和多窗口设置同步。

## 修复与改进

- **截图目标语言**：截图翻译始终保留设置或结果窗中明确选择的目标语言，修复指定日语时被智能语言方向改写的问题。
- **视觉输出净化**：改进结构化 JSON、代码围栏和前置说明解析，拒绝模型回显系统提示词。
- **OCR 请求分流**：视觉翻译使用结构化 JSON 响应，纯 OCR 识别使用纯文本响应，避免响应格式约束互相冲突。
- **DeepSeek 请求**：关闭不需要的 thinking 输出，并设置合理的视觉输出长度上限。
- **设置同步**：截图和悬浮窗读取设置前刷新共享配置，避免语言切换后继续使用旧值。
- **版本显示**：隐私政策弹窗动态显示当前应用版本。

## 升级说明

- 可从 `1.0.3` 直接安装升级到 `1.0.4`。
- 设置、Credential Manager 中的 Provider API Key、历史、缓存和快捷键会继续保留在本机。
- 应用内更新不做静默安装；验证成功后才启动 NSIS 安装程序。

## 安装包

- Windows x64 NSIS 安装包：`TransLoop_1.0.4_x64-setup.exe`
- 更新校验资产：`SHA256SUMS`、`SHA256SUMS.sig`
